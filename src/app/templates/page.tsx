
// This page is no longer in active use as templates are integrated into the Records page (Level D forms).
// It can be deleted or kept for reference. For now, I'll clear its content to avoid confusion.

export default function TemplatesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Templates Page (Deprecated)</h1>
      <p className="text-muted-foreground">
        Document templates (previously Forms) are now managed under the &quot;Records & Forms&quot; section, specifically Level D2 Forms.
      </p>
    </div>
  );
}
